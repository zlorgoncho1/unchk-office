package sn.unchk.office.people.domain;

/**
 * Genre d'une personne (type enumere PostgreSQL {@code genre}).
 * Les valeurs correspondent exactement aux libelles de la base.
 */
public enum Genre {
    homme,
    femme,
    autre
}

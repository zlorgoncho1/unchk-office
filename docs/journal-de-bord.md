# Journal de bord — UNCHK Office

Suivi chronologique des **problèmes rencontrés** et des **solutions appliquées**, tenu au fil de l'avancement.
Objectif : tracer les décisions et éviter de re-buter sur les mêmes obstacles.

| # | Date | Problème rencontré | Cause | Solution appliquée |
|---|------|--------------------|-------|--------------------|
| 1 | 2026-06-08 | Choix du mode Kafka hésitant (ZooKeeper vs KRaft) | Consigne d'abord « pas KRaft », puis recadrée « sans ZooKeeper » | Adoption de **KRaft** (cluster natif), nœud combiné `broker,controller`, extensible en cluster 3 nœuds |
| 2 | 2026-06-08 | Impossible de compiler le backend en local | Pas de **JDK ni Maven** installés (seulement Node + Docker) | Compilation Java **déléguée à un conteneur Maven** (`maven:3.9-eclipse-temurin-21`), cache `.m2` monté en volume |
| 3 | 2026-06-08 | `ng new` échoue : « Angular CLI requires Node ≥ v24.15.0 » | Angular **20** (latest) exige Node 24.15, or l'environnement est en **24.10** | **Pin d'Angular 19** (pleinement capable, compatible Node 24.10 — simples avertissements `EBADENGINE`) |
| 4 | 2026-06-08 | Générateur `m3-theme` : `Unknown arguments` puis fichier mal rangé | Options en **dash-case** (`--primary-color`…) et `--directory` collé comme **préfixe** du nom de fichier | Bons flags + déplacement manuel du fichier vers `src/styles/_theme-colors.scss` |
| 5 | 2026-06-08 | `.dockerignore` de module inopérants et non conformes | Contexte de build = **racine** (Docker ne lit que le `.dockerignore` racine) ; l'un **nommait** `CLAUDE.md`/PDF (interdit) | Suppression des `.dockerignore` de module, création d'un **`.dockerignore` racine conforme** (exclut le lourd, ne nomme aucun fichier interdit) |
| 6 | 2026-06-08 | `ng build` échoue : `@angular/animations/browser` introuvable | `provideAnimationsAsync()` requiert le paquet **`@angular/animations`**, non installé par le scaffold ni par `ng add @angular/material` | `npm install @angular/animations@^19` |
| 7 | 2026-06-08 | Compilation des services : `EntityName() has protected access` (Document, Budget, Notification, Partner…) | Constructeurs sans-argument JPA déclarés `protected`, mais appelés via `new Entity()` depuis le package `service` (hors package `domain`) | Passage des constructeurs sans-argument concernés en `public` (13 entités/projections) |
| 8 | 2026-06-08 | `insertion-service` test : `no suitable method found for thenReturn(List<Object>)` | Piège varargs Java : `List.of(new Object[]{...})` avec **un seul** tableau est étalé en `List<Object>` au lieu de `List<Object[]>` | Témoin de type explicite `List.<Object[]>of(...)` |
| 9 | 2026-06-08 | Conventions de config **divergentes** entre services (DB_URL vs SPRING_DATASOURCE_URL vs DB_HOST ; issuer JWT `unchk-office` vs URL ; identity sur 8081 vs JWKS attendu sur 8080) | 7 agents indépendants, conventions non harmonisées | Normalisation via un bloc d'env partagé dans `docker-compose` (port interne 8080 partout, identifiants DB unifiés, `JWT_ISSUER=http://identity-service:8080`) — affinage runtime à la mise en route |
| 10 | 2026-06-08 | Image OPA : `0.68.0-rootless` introuvable, puis `latest-rootless` **déprécié** et plante au parsing (`authz.rego:17 : if required`) | OPA a basculé en 1.x ; le tag `*-rootless` est gelé sur une vieille version au parseur incompatible | Pin **`openpolicyagent/opa:1.17.0`** (rootless par défaut en 1.x ; `opa check` valide notre politique) |
| 11 | 2026-06-08 | `docker compose build` échoue : timeouts vers Maven Central | Les 9 Dockerfile retéléchargent **chacun** tout l'arbre de dépendances → saturation/timeout de Maven Central | Cache BuildKit partagé `--mount=type=cache,target=/root/.m2,sharing=locked` sur les `RUN mvn` (téléchargement unique, builds suivants rapides) |
| 12 | 2026-06-08 | Images services : `Could not find artifact unchk-office-parent:pom` | Le Dockerfile installe `common` puis build le service, mais le **POM parent** n'est jamais installé dans le `.m2` (résolu seulement via `relativePath`) | Ajout d'un `mvn -N -f pom.xml install` (parent) avant l'install de `common` dans les 7 Dockerfile |
| 13 | 2026-06-08 | Services crashent : `Schema-validation: column email found [citext], expecting [varchar]` | Flyway crée le schéma avec des types PostgreSQL (`citext`, enum, `inet`) non reconnus par la validation Hibernate (`ddl-auto=validate`) | `SPRING_JPA_HIBERNATE_DDL_AUTO=none` (Flyway = source de vérité du schéma) |
| 14 | 2026-06-08 | Services crashent : `BeanDefinitionOverrideException` sur `kafkaTemplate` | `libs/common` fournit un `KafkaTemplate` JSON qui entre en conflit avec l'auto-config Kafka de Boot | `SPRING_MAIN_ALLOW_BEAN_DEFINITION_OVERRIDING=true` (le template custom prime) |
| 15 | 2026-06-08 | people/communication crashent : `JwtDecoder : expected single bean but found 2` | Boot crée `jwtDecoderByJwkKeySetUri` (via `jwk-set-uri`) **en plus** du `decodeurJwt` de `common` → chaîne de sécurité Boot ambiguë | `@Primary` sur `decodeurJwt` dans `libs/common` |
| 16 | 2026-06-08 | `docker compose up` : `Bind for 0.0.0.0:8085 failed` (kafka-ui) | Port hôte 8085 déjà occupé | Port kafka-ui déplacé en 8090 |
| 17 | 2026-06-08 | Login via gateway → 401 ; login direct → 400 `motDePasse obligatoire` | (a) le gateway exige un JWT sur `/api/identity/auth/login` ; (b) le DTO attend `{email, motDePasse}` (champs FR) | (a) `permitAll` sur `/api/identity/auth/**` dans `SecurityConfig` **et** dans la liste publique du filtre OPA ; (b) payload corrigé |
| 18 | 2026-06-08 | academic/admin/document/insertion → 401 `WWW-Authenticate: Basic` | Ces services n'avaient **pas de chaîne JWT** active et retombaient sur la sécurité Basic par défaut de Boot (config sécurité hétérogène entre agents) | Activation uniforme du resource-server JWT de Boot via env `spring.security.oauth2.resourceserver.jwt.jwk-set-uri` + `issuer-uri` (sans rebuild) |
| 19 | 2026-06-08 | Aucun compte pour se connecter (login impossible) | Identity ne seedait aucun utilisateur | Migration **V3 seed admin** (BCrypt `$2a$`, idempotente) : `admin@unchk.sn` / `Admin123!`, rôle admin |
| 20 | 2026-06-08 | 3 tests unitaires KO (academic/document/communication) + 2 E2E KO | Tests générés sans exécution : ID null du mock `save`, `ObjectMapper` sans module JSR310 ; sélecteur Playwright `getByLabel('Mot de passe')` ambigu (input + bouton « Afficher ») | Mock `save` affecte un ID ; `new ObjectMapper().findAndRegisterModules()` ; sélecteurs E2E par `formControlName`. **Suite verte : backend (9 modules) + E2E 3/3** |
| 21 | 2026-06-08 | **Gateway renvoie des réponses au corps VIDE** (`content-length: 0`) : dashboards vides malgré des données réelles (POST OK mais GET sans corps) | Le filtre OPA lisait `ReactiveSecurityContextHolder` / `exchange.getPrincipal()`, **non propagés dans un GlobalFilter Spring Cloud Gateway** → contexte vide → refus + `setComplete()` qui vide le corps. Détecté car les smoke-tests ne vérifiaient que le code HTTP, pas le corps | Le filtre OPA **décode lui-même le JWT** (déjà validé) via `ReactiveJwtDecoder` pour extraire sujet + rôles ; la corrélation pose son en-tête via `beforeCommit`. Données circulent bout-en-bout (dashboards remplis) |
| 22 | 2026-06-08 | Notifications WebSocket « Hors ligne » : mismatch de protocole (frontend WebSocket brut vs backend STOMP), handshake refusé (le navigateur ne pose pas d'en-tête `Authorization`), et 2 chaînes de sécurité dans le service (resource-server Boot vs `common`) bloquant `/ws` | Réécriture backend en **WebSocket simple** (handler texte) ; JWT passé en `?access_token` et validé au handshake via `JwtDecoder` ; **chaîne de sécurité dédiée `/ws/**` en priorité haute** (permit) ; gateway : `/ws/notifications` public (permit + exclusion OPA) | WS connecté bout-en-bout (session ouverte, push JSON ciblé par utilisateur). NB : une notification ne cible que des utilisateurs propagés par événement — l'admin seedé en SQL n'émet pas d'`identity.users` → 0 destinataire |
| 23 | 2026-06-08 | `docker compose up` ne démarrait pas le frontend (lancé à part via `ng serve`) ; au build, `npm ci` échoue (lock désynchronisé : binaires esbuild multi-plateformes manquants) | Frontend **conteneurisé** (build Node multi-étapes → nginx + routing SPA) ajouté au compose (port 4200, `depends_on` gateway) ; `npm install` tolérant au lieu de `npm ci` dans le Dockerfile | **`docker compose up` démarre toute l'application** (frontend + backend) ; app directement accessible sur http://localhost:4200 |
| 24 | 2026-06-08 | Création de comptes via l'API (5 rôles) en **500** : topic Kafka `identity.users` absent (auto-create off, pas de bean `NewTopic` côté identity) ; puis colonne enum PG `role_code` vs varchar Hibernate (clé composite : `@ColumnTransformer` ignoré) | Bean `NewTopic(identity.users)` ajouté ; `stringtype=unspecified` sur l'URL JDBC d'identity (PG infère et caste varchar→enum) | Les **5 comptes de rôles** (admin/administratif/enseignant/appui/étudiant) se créent et se connectent ; événements propagés aux projections |
| 25 | 2026-06-08 | Audit de sécurité **adversarial** (workflow d'agents) : **IDOR objet** confirmé — un rôle à large lecture (`GET /api/**`) lisait n'importe quel document/compte-rendu par id, hors de sa `visibility` | La règle OPA `allow` (RBAC route) court-circuitait l'ABAC. Ajout d'une règle **`allow_objet`** (ABAC stricte : admin/propriétaire/visibilité, sans `route_allowed`) interrogée par le guard objet de tous les services ; refus mappé en 403 (`@ResponseStatus` sur `AccesRefuseException`) | IDOR fermée (8/8 conformes à la visibilité, codes 200/403 propres) ; RBAC intact (matrice 5 rôles validée) |
| 26 | 2026-06-08 | Réalisation des pages internes (placeholders) avec cohérence DA stricte sur les tables, **en parallèle sans conflit** | Composant **table réutilisable** `unchk-data-table` (colonnes typées, pastilles, filtre, états) + workflow de 13 agents (un par page, fichiers cloisonnés). Build KO ensuite : attribut booléen nu → `booleanAttribute` sur les inputs partagés ; `as` sur `@else if` → `@if` imbriqué | **13 pages** livrées (DA homogène), build vert, zéro erreur API |
| 27 | 2026-06-08 | `/api/etudiants/me` **404** : le consumer `identity.users` n'alimentait que `IdentityUserRo`, jamais `student.user_ref` | Le consumer renseigne `student.user_ref` quand `personRef` vise un étudiant (no-op pour le personnel) ; test du consumer adapté | `/me` résout la fiche (200) pour les comptes propagés |
| 28 | 2026-06-08 | Tableaux incohérents (dashboards Material « boxy » vs pages features) + lignes trop aérées + vue détail en dialog tronquée | Unification du style `.home-table` sur `unchk-data-table` ; densité harmonisée (échelle 4/8/12/16/20/24) ; vue détail passée en **drawer latéral** (`mat-dialog-content` pour padding/scroll corrects) | Tables identiques partout, denses ; drawer propre ; 0 anomalie E2E |
| 29 | 2026-06-08 | **Gestion CRUD** : besoin de formulaires créer/modifier/supprimer cohérents sur tous les modules, vite et en parallèle | Fondation partagée : `unchk-form-drawer` (formulaire config-driven en drawer) + `unchk-confirm-dialog` + actions du `data-table` ; **1 référence vérifiée (partenaires)** puis workflow de 6 agents (1/domaine, cloisonné par service) qui répliquent le patron + découvrent les DTO backend | CRUD livré sur partenaires, people, formations, stages, budgets ; create sur réunions/CR ; modifier/supprimer sur documents |
| 30 | 2026-06-08 | Le `unchk-data-table` **ne s'instanciait plus** (composant absent du DOM, état vide) dès qu'on liait l'input `actions` (type générique `ActionTable<T>[]` avec une **propriété fonction** `visible?: (ligne: T) => boolean`) — sans erreur runtime | Remplacement de l'input tableau générique par des **inputs booléens** `modifiable`/`supprimable` + outputs `modifier`/`supprimer` (même mécanisme que `detaillable` qui fonctionnait). Diagnostic par bissection (input vs output, debug DOM) | Table rend de nouveau ses lignes + boutons ; CRUD partenaires/étudiants vérifié end-to-end |
| 31 | 2026-06-08 | Côté backend, `communication-service` (réunions, comptes-rendus) et `admin-service` (budgets) n'exposaient **que POST/GET** — pas de PUT/DELETE pour la gestion complète | Ajout des endpoints `modifier`/`supprimer` event-driven (émission d'événements Updated/Deleted comme la création), via agents cloisonnés par service | Gestion complète (modifier/supprimer) sur réunions, comptes-rendus et budgets |
| 32 | 2026-06-08 | Analyse d'écart vs cahier des charges : 3 exigences câblées seulement côté backend — **exports PDF/Excel**, **upload de documents** (multipart MinIO), **page Emplois du temps** (route → placeholder) | Implémentation parallèle via 3 agents sur **worktrees git isolés** (cloisonnement strict : fichiers disjoints), puis intégration par **cherry-pick** des 3 commits dans `main` | Exports PDF (1.9 ko) + Excel (3.9 ko) téléchargés ; upload de fichier OK (8→9) ; page Emplois du temps réelle (sélecteur de formation + créneaux). Conforme à l'énoncé |
| 33 | 2026-06-08 | Audit de couverture strict (6 agents, exigence par exigence) : termes **explicites** de l'énoncé manquants — type CR « Rencontre », catégories doc (courrier arrivé/départ, notes internes/externes, note administrative), **montant** du financement, fiche étudiant (Formation/Diplômes/Autres formations), registre de contact, **budget réalisé** ; + notif circulaire cassée + publication CR non déclenchable | Correction parallèle (6 worktrees, un par domaine) : enums + migrations Flyway **additives** (ALTER TYPE … ADD VALUE), DTO/entités, formulaires dynamiques, dialog diplômes, lignes budgétaires ; auto-publication du CR à la création ; `ConsommateurDocument` notifie sur circulaire ; historique notifications chargé (REST + WS) | Toutes les exigences explicites couvertes ; 5 services rebâtis, migrations OK, build vert |
| 34 | 2026-06-08 | Régressions des corrections : `GET /api/communication/notifications` **403** pour appui/étudiant (chacun doit voir LES SIENNES) ; registre de contact **403** pour appui (ABAC objet du domaine insertion) ; enseignant routé à tort vers le registre | OPA : règle globale « notifications personnelles » (tout rôle authentifié, GET/PATCH) ; `allow_objet` domaine insertion pour appui/administratif ; lecture des étudiants pour appui ; retrait d'enseignant du registre (outil appui-insertion) | Balayage final **5 rôles × 16 routes = 0 erreur** (réseau/console), RBAC cohérent |
| 35 | 2026-06-08 | Revue visuelle Playwright page par page : colonnes vides (Statut CR, Organisateur réunion, Auteur), Formation étudiant non renseignée, et surtout **projections `identity_user_ro`/`people_staff_ro` de communication VIDES** → la résolution des destinataires par rôle ne trouvait personne (**les notifications ne partaient pas**) | Statut CR déduit de `publishedAt` (front) ; résolution de nom avec **fallback `identity_user_ro`** (organisateur/auteur non-staff, ex admin) ; peuplement des projections depuis les sources + rattachement des organisateurs/auteurs à de vrais agents ; `formation_ref` seedé sur les étudiants | Statut Publié/Brouillon affiché ; organisateurs résolus (0 vide) ; **notification de bout en bout vérifiée** (CR publié → destinataire reçoit l'alerte) ; formation visible |

| 36 | 2026-06-11 | Revue visuelle approfondie (5 rôles, page par page, formulaires inclus) : **cartes voisines mal alignées** (« Ma fiche » bien plus courte que le graphe voisin), **montants des tables d'accueil alignés à gauche**, KPI « F CFA » coupé en fin de ligne, **années avec séparateur de milliers** (« 2 024 »), tailles de fichiers en octets bruts, **graphe « effectifs formés par genre » vide** sur la page Statistiques (appui), accents manquants sur les données personnel | UI : `section-card`/`chart-card` en `height: 100%` (les cartes remplissent la cellule étirée par `align-items: stretch` → bas alignés) ; préfixe `.home-table` sur `.aligne-droite` (passe devant la spécificité Material) ; espace insécable dans `formaterMontantCompact` ; colonnes année en type `texte` (pas de groupement) ; `formaterTaille` (o/Ko/Mo/Go). OPA : `appui-insertion` peut lire `/api/academic/formations` (la stat d'insertion croise les effectifs de formation, sinon 403 silencieux). Données : accents corrigés dans `staff`/`student_diplomas` (Maître de Conférences, Réseaux & Sécurité, Développement Web…) | 5 rôles revus, alignement cohérent (bas de cartes coïncidents mesurés), montants à droite, graphe par genre peuplé (axe réel), libellés accentués partout. Reproductibilité du jeu de démo traitée au #37 |
| 37 | 2026-06-12 | **Jeu de démo non reproductible** : personnel, étudiants, formations, emplois du temps, réunions, comptes rendus, documents, partenaires, stages, budgets, comptes de connexion des 5 rôles + projections CQRS avaient été insérés en direct (API/SQL) ; un `docker compose down -v` repartait à vide (seul l'admin était semé en V3). Risque : un évaluateur clonant le dépôt n'aurait aucune donnée | Données extraites de la base de démo (déjà cohérente, **UUID conservés** → références croisées valides), converties en **`pg_dump --column-inserts --on-conflict-do-nothing`** puis versionnées en `V*__seed_demo.sql` par service. FK neutralisées pendant le chargement (`session_replication_role`, `unchk` superuser → ordre indifférent). Objets MinIO (8 PDF documents/courriers) extraits dans `platform/minio/seed/` et chargés par `minio-init`. Validé **en isolation** (schéma+seed sur base neuve, idempotence) puis **end-to-end** : `down -v` → `up` → comptes identiques à l'octet près sur les 7 bases | Reproductible à vide : identity 5/5/11, people 8/11/3, academic 6/10, communication 4/5/9, document 8, insertion 5/5/10, admin 3/22 ; 8 objets MinIO rechargés ; accents préservés (seed dumpé après correction). UI revérifiée : **5 rôles × 21 pages = 0 erreur** console/réseau, données présentes partout. Comptes démo : `administratif@`/`enseignant@`/`appui@`/`etudiant@unchk.sn` (`Demo1234!`) |

| 38 | 2026-06-13 | Audit **design multi-agents** (4 workflows, 188 captures Playwright × 5 rôles × états : principal/création/détail/édition/suppression + thème sombre) → 118 défauts. Faux positifs écartés après **vérification** (Réunions « 50% » = artefact de capture ; « dégradé » de bouton inexistant) | Corrections à la racine (composants partagés = un fix partout) : `MAT_FORM_FIELD_DEFAULT_OPTIONS` global (outline + floatLabel:always) → labels homogènes création/édition/login ; `confirm-dialog` bouton destructif en **rouge** (token MDC, la palette warn Material n'était pas rouge) ; `data-table` filtre avec gouttière 16px + colonne Actions **sticky** ; détails humanisés (« Validé » au lieu de « valide ») + titre contextuel ; `registre-contact` UUID → select formation ; `paramètres` UUID retiré ; thème sombre (logo lisible, nav active translucide, surfaces détachées) ; `LOCALE_ID=fr-FR` | 6 commits ; revues visuelles concluantes ; smoke 5 rôles = 0 erreur |
| 39 | 2026-06-13 | Audit **transversal multi-agents** (RBAC/IDOR, pré-remplissage édition, logique métier, UI). Trouvailles majeures : **IDOR critique** (réunions GET/PUT/DELETE/{id} sans contrôle d'accès objet → tout authentifié modifiait/supprimait n'importe quelle réunion) ; **bug de glob OPA** (`/api/X/**` ne matche jamais la collection `/api/X` → enseignant ne pouvait pas créer de CR, administratif ne pouvait pas uploader) ; **excès/manques de droits** (POST sans PUT/DELETE) ; **formulaires d'édition à moitié vides** (dates ISO non tronquées, CR type/date/contenu non pré-remplis) ; **logique métier absente** : CR sans contenu affiché (modèle front lisait `ownerName` alors que le DTO renvoie `authorName`/`body`), documents non téléchargeables (endpoint backend prêt mais pas de bouton), réunions en ligne sans bouton « Rejoindre » | Sécurité : `@VerifieAccesObjet` (read/update/delete) sur `ReunionController` + attributs ABAC réunion (propriétaire=createdBy) ; RBAC réécrit (chaque rôle gère **pleinement** son domaine, méthode `*`, + chemins de collection explicites) ; l'ABAC objet reste la garde fine. Métier : `data-table` étendu (action `telechargeable`, type colonne `lien`, option `masquerEnTable`) → bouton Télécharger (URL présignée MinIO), bouton vert « Rejoindre » pour les visios, **contenu réel du CR** affiché en détail (+ Auteur enfin renseigné). Pré-remplissage des dates corrigé (6 composants) | Vérifié : IDOR fermé (enseignant→403, admin→200) ; enseignant crée CR+réunions, administratif gère personnel+upload, étudiant reste lecture (403 POST) ; « Rejoindre » sur le webinaire, contenu CR visible, édition CR pré-remplie (type/date/contenu) ; téléchargement = URL présignée. **Smoke 5 rôles × ~8 pages = 0 erreur API**. NB : `document-service` retombé (Exited 255, instabilité Docker) — relancé |
| 40 | 2026-06-15 | **Audit de conformité multi-agents contre l'énoncé PDF** (8 agents + critique de complétude, recoupé sur disque). Écart majeur confirmé : **module Administration à moitié creux** — `mails` (courrier arrivé/départ) et `admin_communiques` (notes de service / circulaires) n'existaient qu'en **tables SQL**, sans entité Java, controller ni UI (un agent avait même **halluciné** des fichiers `Mail.java`/`AdminCommunique.java` inexistants → vérification disque obligatoire avant de conclure). Autres écarts : **export budget PDF/Excel** présent au backend mais **non câblé au frontend** ; notes de service / circulaires ne **notifiaient pas** | Implémentation de la couche métier+UI manquante. Courrier `/api/admin/mails` et communiqués `/api/admin/communiques` : entités, repositories, DTO, services, REST CRUD, ABAC anti-IDOR (`FournisseurAttributsAdmin`, types `courrier`/`communique`), suppression logique, cycle de vie/statut. **Publication d'un communiqué → notification automatique** par rôle ciblé (nouveau topic Kafka `admin.communiques` produit par admin-service, consommé par communication-service → `ServiceNotification`). Frontend : écrans Courrier & Communiqués (data-table + drawers + action « Publier »), export budget câblé (Blob authentifié) dans le drawer de détail, **action générique réutilisable** ajoutée au `data-table`. Seed reproductible V3 (4 courriers, 3 communiqués) | Build Docker (2 services + frontend) OK. Smoke : 4 courriers / 3 communiqués seedés, création courrier + PATCH statut 200, **publication brouillon → notification créée** (chaîne Kafka admin→communication de bout en bout), **export budget PDF 200** (`%PDF-`, ~1,7 ko), RBAC étudiant→courrier **403** / administratif→communiqués **200** |
| 41 | 2026-06-15 | **Recette vidéo des 5 acteurs** (pilote Playwright : login → chaque page du rôle → détail/création/édition/action métier/export/suppression, bannière narrative, conscient des droits d'écriture). Elle a révélé un **vrai bug** : toute **création d'insertion** (partenaire, stage, contact, situation) échouait en **HTTP 500**. Cause : l'insertion-service était le **seul** service à ne pas déclarer son topic Kafka producteur ; avec l'auto-création désactivée, `insertion.events` n'existait pas → `kafkaTemplate.send` bloquait `max.block.ms` (`UNKNOWN_TOPIC_OR_PARTITION`) puis `TimeoutException` → 500. Le **seed SQL ne passant pas par le producteur**, le bug restait invisible aux smoke tests sur données semées | Ajout de `ConfigurationTopics` (bean `NewTopic` pour `INSERTION_EVENTS`), aligné sur academic/communication/admin. Rebuild + redéploiement insertion-service | Vérifié : 0 `UNKNOWN_TOPIC`, **POST partenaire/stage → 201** (était 500), DELETE 204. Vidéos régénérées (admin, appui-insertion) après correctif. Trouvaille UI annexe (non bloquante) : certaines pages exposent des actions d'écriture/détail à des rôles sans le droit objet → le backend renvoie **403** (sécurité OK), mais boutons à masquer côté UI (suivi proposé) |

> Ce journal est mis à jour à chaque nouvel obstacle non trivial. Voir aussi `docs/architecture.md`,
> `docs/security.md` et `docs/database.md` pour les décisions de conception.
